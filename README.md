# portfolio-app (guscio pubblico)

Solo l'interfaccia dell'app di gestione investimenti. **Nessun dato qui dentro.**
I dati stanno nel repo privato `portfolio` e vengono letti/scritti dall'app via API
GitHub con il tuo token personale (inserito in Impostazioni).

Aggiornare il guscio: ricopia `docs/index.html` dal repo `portfolio` in questo repo.
